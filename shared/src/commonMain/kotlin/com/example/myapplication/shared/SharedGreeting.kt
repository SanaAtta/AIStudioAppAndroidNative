package com.example.myapplication.shared

/** Simple shared entry for iOS / Android to verify the KMP module is linked. */
object SharedGreeting {
    fun hello(): String = "Shared KMP module is ready"
}
