# Local API keys fallback (used when Firebase Remote Config is empty or fetch fails).
# Firebase Remote Config parameter names must use underscores:
#   pollinations_api_key
#   deapi_api_key
#   groq_api_key
#
# Legacy names below still work as local build fallback:

pollinations.api.key=
deapi.api.key=
groq.api.key=
