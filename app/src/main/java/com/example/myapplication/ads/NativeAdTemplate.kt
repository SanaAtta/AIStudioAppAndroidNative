package com.example.myapplication.ads

/**
 * Native ad layout variant selected from Remote Config (`native_ad_with_media` /
 * `native_ad_without_media` per screen section).
 */
enum class NativeAdTemplate {
    DISABLED,
    WITHOUT_MEDIA,
    WITH_MEDIA,
}
